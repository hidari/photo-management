{
  "tasks": {
    "test": "deno check tools/**/*.ts tests/**/*.ts && deno test --allow-read --allow-write --coverage=coverage/ && deno coverage coverage/ --lcov --output=coverage.lcov && deno run --allow-read --allow-run tests/helpers/check-coverage.ts 50",
    "readme": "deno run --allow-read --allow-write tools/generate-readme.ts",
    "dirs": "deno run --allow-read --allow-write tools/generate-directories.ts"
  },
  "nodeModulesDir": "auto",
  "imports": {
    "types/": "./types/"
  }
}
